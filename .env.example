# Vercel Blob — public store token (set via Vercel dashboard, do not commit the real value)
BLOB_READ_WRITE_TOKEN=

# Azure AD / Microsoft Entra ID — for SSO login
AZURE_CLIENT_ID=831df3f9-82c0-4107-83b0-1f59df905a78
AZURE_TENANT_ID=62ccb864-6a1a-4b5d-8e1c-397dec1a8258
AZURE_CLIENT_SECRET=          # create in Azure portal after M365 approval

# Flask session signing key — generate with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=

# OAuth redirect URI (default shown — override for local dev)
REDIRECT_URI=https://rvn-usage-tracker.vercel.app/auth/callback
